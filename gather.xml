<Response>
    <Say>
        <%= (Digits == '1') ? 'You selected sales. Please hold while we connect you.' : '' %>
        <%= (Digits == '2') ? 'You selected support. Please hold while we connect you.' : '' %>
        <%= (!['1', '2'].includes(Digits)) ? 'Sorry, I did not understand that choice.' : '' %>
    </Say>
    <%= (!['1', '2'].includes(Digits)) ? '<Redirect>https://nothinormuch.github.io/DarkRoom_Project_Library/voice.xml</Redirect>' : '' %>
</Response>
